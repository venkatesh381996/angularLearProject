import { CustomerDirDirective } from './customer-dir.directive';

describe('CustomerDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerDirDirective();
    expect(directive).toBeTruthy();
  });
});
